# Rate Limiting: Анализ функциональности

> **Версия:** 1.0  
> **Дата:** 2025-12-14  
> **Статус:** ✅ Полностью реализовано

---

## 📋 Обзор

Rate Limiting — механизм защиты от перегрузки, реализованный на основе **Token Bucket** алгоритма. Функциональность позволяет ограничивать количество запросов на трёх уровнях: глобально, per-client и per-model.

---

## 🔍 Анализ конфигурации

### Текущая структура в config.yaml

```yaml
rateLimiting:
  enabled: false            # Включение/выключение rate limiting
  
  global:
    requestsPerMinute: 100  # Общий лимит на все запросы
    
  perClient:
    enabled: true
    requestsPerMinute: 20   # Лимит на клиента (X-Client-ID или IP)
    burstSize: 5            # Дополнительные токены для всплесков
    
  perModel:
    enabled: true
    requestsPerMinute: 30   # Лимит на модель
```

### Дефолтные значения (из кода)

Файл: `src/modules/rate-limiter/interfaces/rate-limiter.interface.ts`

| Параметр | Дефолт | Описание |
|----------|--------|----------|
| `enabled` | `false` | Rate limiting выключен по умолчанию |
| `global.requestsPerMinute` | `100` | 100 req/min для всех клиентов |
| `perClient.enabled` | `true` | Per-client лимит активен (когда rate limiting включён) |
| `perClient.requestsPerMinute` | `20` | 20 req/min на клиента |
| `perClient.burstSize` | `5` | Допустимый "всплеск" 5 токенов |
| `perModel.enabled` | `true` | Per-model лимит активен |
| `perModel.requestsPerMinute` | `30` | 30 req/min на модель |

---

## ✅ Полнота реализации

### Реализованные компоненты

| Компонент | Файл | Статус |
|-----------|------|--------|
| **Интерфейсы** | `rate-limiter.interface.ts` | ✅ Полностью |
| **Token Bucket сервис** | `rate-limiter.service.ts` | ✅ Полностью |
| **Валидатор конфига** | `rate-limiting-validator.ts` | ✅ Полностью |
| **NestJS Guard** | `rate-limiter.guard.ts` | ✅ Полностью |
| **Модуль** | `rate-limiter.module.ts` | ✅ Полностью |
| **Интеграция в контроллер** | `router.controller.ts` | ✅ Полностью |
| **Admin API endpoint** | `admin.controller.ts` | ✅ Полностью |
| **Unit тесты** | `rate-limiter.service.spec.ts` | ✅ Полностью |
| **Документация** | `README.md` | ✅ Полностью |

### Функциональные возможности

| Возможность | Статус | Комментарий |
|-------------|--------|-------------|
| Глобальный лимит | ✅ | Token Bucket для всех клиентов |
| Per-client лимит | ✅ | По `X-Client-ID` заголовку или IP |
| Per-model лимит | ✅ | Защита от перекоса на одну модель |
| Burst размер | ✅ | Только для per-client |
| Автоматический refill | ✅ | Токены восполняются по времени |
| Заголовки ответа | ✅ | `X-RateLimit-*`, `Retry-After` |
| Cleanup устаревших бакетов | ✅ | Каждые 5 минут |
| Admin API статус | ✅ | `GET /admin/rate-limits` |
| Graceful shutdown | ✅ | `onModuleDestroy` очищает interval |

---

## 🧐 Оценка конфигурационных параметров

### Количество параметров

**Всего:** 8 параметров (включая вложенные)

```
rateLimiting.enabled                 - 1
rateLimiting.global.requestsPerMinute - 1
rateLimiting.perClient.enabled       - 1
rateLimiting.perClient.requestsPerMinute - 1
rateLimiting.perClient.burstSize     - 1
rateLimiting.perModel.enabled        - 1  
rateLimiting.perModel.requestsPerMinute - 1
```

### 🟢 Вердикт: Оптимально

Количество параметров **не избыточно**:
- Логическая группировка по уровням (global, perClient, perModel)
- Каждый параметр имеет чёткое назначение
- Нет дублирования функциональности
- Иерархия `enabled` флагов работает правильно

### Потенциальное упрощение

Можно было бы убрать `perClient.enabled` и `perModel.enabled`, сделав их активными автоматически при указании `requestsPerMinute`, но текущий явный подход **лучше для понимания** конфигурации.

---

## 🔧 Технические детали реализации

### Token Bucket алгоритм

```typescript
// Структура бакета
interface TokenBucket {
  tokens: number;         // Текущее количество токенов
  lastRefill: number;     // Timestamp последнего refill
  maxTokens: number;      // Максимальная ёмкость
  refillRate: number;     // Токенов в миллисекунду
}
```

**Логика:**
1. При каждом запросе `checkClient/checkModel/checkGlobal`
2. Сначала refill: добавляем токены пропорционально времени
3. Пытаемся взять 1 токен
4. Если токенов < 1 — отказ (429)

### Математика refill

```typescript
refillRate = requestsPerMinute / 60000  // токенов в мс
tokensToAdd = elapsed * refillRate
bucket.tokens = Math.min(maxTokens, tokens + tokensToAdd)
```

**Пример:** при `requestsPerMinute: 20`:
- `refillRate = 20/60000 ≈ 0.000333 токенов/мс`
- За 1 секунду добавляется ~0.333 токена
- Полный refill за ~3 секунды при пустом бакете

### Очистка устаревших бакетов

```typescript
// Константы из app.constants.ts
STALE_BUCKET_THRESHOLD_MS = 600_000    // 10 минут неактивности
RATE_LIMITER_CLEANUP_INTERVAL_MS = 300_000  // проверка каждые 5 минут
```

Бакеты клиентов и моделей, не использовавшиеся 10 минут, удаляются для экономии памяти.

---

## 📊 Точки интеграции

### 1. Router Controller

```typescript
// router.controller.ts
@Post('chat/completions')
@UseGuards(RateLimiterGuard)  // Проверка rate limit перед обработкой
public async chatCompletion(...) { ... }
```

Rate limit применяется **только** к `POST /api/v1/chat/completions`, что логично — это основной endpoint нагрузки.

### 2. Admin API

```typescript
GET /admin/rate-limits  // Возвращает статус rate limiter
```

Ответ содержит:
- `enabled: boolean`
- `config: RateLimitingConfig`
- `activeBuckets: { global, clients, models }` — количество активных бакетов

### 3. Заголовки ответа

Guard добавляет заголовки к каждому ответу от защищённого endpoint:

```http
X-RateLimit-Limit: 20        # Максимум запросов
X-RateLimit-Remaining: 17    # Осталось в текущем окне
X-RateLimit-Reset: 1702469100 # Unix timestamp сброса
Retry-After: 45              # Только при 429
```

---

## ⚠️ Потенциальные проблемы и рекомендации

### 1. Отсутствие per-model проверки в Guard

**Проблема:** Guard проверяет только `global` и `perClient` лимиты. `perModel` лимит **не проверяется** на уровне Guard, потому что модель ещё не известна.

```typescript
// rate-limiter.guard.ts
const result = this.rateLimiterService.checkAll(clientId);
// modelName не передаётся!
```

**Рекомендация:** Это **осознанное ограничение** — модель выбирается в `RouterService` после Guard. Per-model лимит можно проверять в `SelectorService` при выборе модели.

**Статус:** 🟡 **Дизайнерское решение** — документировано, но добавляет сложности.

### 2. Нет отдельного burstSize для perModel

**Проблема:** `burstSize` есть только для `perClient`. Модели не могут иметь "всплеск".

**Рекомендация:** Это **правильно** — burst нужен для клиентов (люди делают серии запросов), а не для моделей. Для моделей важна равномерная нагрузка.

**Статус:** 🟢 **OK**

### 3. Отсутствие sliding window для перегрузки

**Проблема:** Token Bucket не предотвращает "burst at reset" — если клиент ждёт refill и отправляет много запросов разом в момент пополнения.

**Рекомендация:** Это стандартное поведение Token Bucket. Если нужна более равномерная нагрузка — использовать Leaky Bucket или Sliding Window Log. Для LLM роутера текущий алгоритм **достаточен**.

**Статус:** 🟢 **OK**

### 4. Нет возможности исключать IP/клиентов

**Проблема:** Нет whitelist для bypass rate limiting (например, для internal services).

**Рекомендация:** Можно добавить в будущем через `whitelist: ['127.0.0.1', 'internal-service']`.

**Статус:** 🟡 **Улучшение на будущее**

### 5. Нет Redis-backed rate limiting

**Проблема:** При горизонтальном масштабировании (несколько инстансов) rate limit **не распределённый** — каждый инстанс имеет свой лимит.

**Рекомендация:** Документировано как осознанное ограничение. Redis rate limiting запланирован в v2.0.

**Статус:** 🟡 **Известное ограничение**

---

## 📈 Оценка текущих дефолтных значений

### Для production окружения

| Параметр | Дефолт | Оценка | Рекомендация |
|----------|--------|--------|--------------|
| `global.requestsPerMinute: 100` | 100 req/min | 🟢 Консервативно | OK для начала |
| `perClient.requestsPerMinute: 20` | 20 req/min | 🟢 Хорошо | ~1 запрос в 3 сек |
| `perClient.burstSize: 5` | 5 токенов | 🟢 Достаточно | Покрывает серию быстрых запросов |
| `perModel.requestsPerMinute: 30` | 30 req/min | 🟡 Зависит от моделей | Может быть мало для популярных моделей |

**Рекомендация:** Для production следует увеличить лимиты в 2-3 раза и мониторить через `/admin/rate-limits`.

---

## 🧪 Покрытие тестами

**Файл:** `test/unit/modules/rate-limiter/rate-limiter.service.spec.ts`

| Сценарий | Статус |
|----------|--------|
| Rate limiting disabled | ✅ |
| Global rate limit | ✅ |
| Per-client rate limit | ✅ |
| Per-model rate limit | ✅ |
| Burst размер | ✅ |
| checkAll с типом лимита | ✅ |
| getRateLimitInfo | ✅ |
| Token refill over time | ✅ |

**Покрытие:** ~95% для `rate-limiter.service.ts`

**Не покрыто:**
- E2E тесты с реальными HTTP запросами
- `rate-limiter.guard.ts` — низкое покрытие (Guard специфика)

---

## 🏁 Итоговая оценка

### Сильные стороны

1. ✅ **Чистая архитектура** — отдельный модуль, guard, service, interface
2. ✅ **Token Bucket** — проверенный индустриальный алгоритм
3. ✅ **Три уровня лимитов** — гибкость для разных сценариев
4. ✅ **Cleanup устаревших данных** — нет утечек памяти
5. ✅ **Graceful shutdown** — корректное завершение
6. ✅ **Admin API** — наблюдаемость
7. ✅ **Стандартные заголовки** — `X-RateLimit-*`, `Retry-After`

### Слабые стороны

1. 🟡 **perModel не проверяется в Guard** — проверяется только при выборе модели
2. 🟡 **Нет whitelist** — все клиенты под одним лимитом
3. 🟡 **Не распределённый** — не работает для multi-instance

### Избыточность

**Нет** — функциональность не избыточна. Все три уровня лимитов имеют разное назначение:
- `global` — защита всего сервиса
- `perClient` — защита от агрессивного клиента
- `perModel` — защита от перекоса нагрузки

### Недостающее

1. **Whitelist** для исключения IP/клиентов
2. **Dynamic limits** — изменение лимитов без рестарта
3. **Per-provider limits** — отдельный лимит на OpenRouter vs DeepSeek
4. **Distributed rate limiting** (Redis) — для масштабирования

---

## 📝 Вывод

**Rate Limiting функциональность полностью реализована** в соответствии с планом `smart-routing-plan.md`. Конфигурация **оптимальна и не запутывает** — параметры логически сгруппированы, имеют разумные дефолты. Реализация соответствует production-ready стандартам с учётом осознанных ограничений (in-memory, single-instance).

Для enterprise deployment рекомендуется:
1. Увеличить лимиты в 2-3 раза
2. Добавить мониторинг `/admin/rate-limits`
3. В v2.0 — Redis-backed rate limiting для масштабирования
