# Аудит обработки ошибок провайдера и логики ретраев

**Дата:** 2025-12-14  
**Версия:** 3.0 (после внесения изменений)  
**Статус:** Завершен

## Резюме

Проведен детальный аудит обработки ошибок провайдера, логики ретраев, переключения на другие модели и работы Circuit Breaker. Выявленные проблемы **исправлены**, документация **обновлена**.

### ✅ Внесенные изменения

1. **4xx ошибки (кроме 429 и 404)** — исключены из Circuit Breaker статистики
2. **Сетевые ошибки** — добавлена дифференцированная обработка:
   - `ENETUNREACH`, `ECONNRESET` — ретрай на той же модели
   - `ECONNREFUSED`, `EHOSTUNREACH`, `ENOTFOUND`, `ETIMEDOUT` — переключение на другую модель
3. **Документация** — полностью обновлена и соответствует коду

---

## 1. Итоговая логика обработки ошибок

### Обработка ошибок и fallback

1. При ошибке **429** (Rate Limit) — ретрай до `rateLimitRetries` раз с задержкой + jitter (на той же модели)
2. При **сетевых ошибках** (ENETUNREACH, ECONNRESET) — ретрай до `rateLimitRetries` раз (проблема может быть временной)
3. При ошибках **4xx** (кроме 429) — прервать выполнение, вернуть ошибку клиенту
4. При ошибках **5xx, timeout** — переключиться на следующую модель
5. При **сетевых ошибках провайдера** (ECONNREFUSED, EHOSTUNREACH, ENOTFOUND) — переключиться на следующую модель
6. Повторяем до `maxRetries` попыток (переключение между моделями)
7. Если все бесплатные модели исчерпаны и `fallback.enabled = true` — используем платную модель

### Таблица обработки HTTP ошибок

| Код ошибки | Действие | Circuit Breaker | Прерывает цикл? |
|------------|----------|------------------|-----------------|
| 404 (Not Found) | Модель не существует | `PERMANENTLY_UNAVAILABLE` | ✅ Да |
| 429 (Rate Limit) | Ретрай с задержкой + jitter | ✅ Учитывается | ❌ Нет (ретрай) |
| 5xx (500-599) | Переключение | ✅ Учитывается, `OPEN` после threshold | ❌ Нет |
| Timeout | Переключение | ✅ Учитывается, `OPEN` после threshold | ❌ Нет |
| 400 (Bad Request) | Вернуть ошибку клиенту | ❌ **Не влияет** | ✅ Да |
| 401 (Unauthorized) | Вернуть ошибку клиенту | ❌ **Не влияет** | ✅ Да |
| 403 (Forbidden) | Вернуть ошибку клиенту | ❌ **Не влияет** | ✅ Да |
| Другие 4xx | Вернуть ошибку клиенту | ❌ **Не влияет** | ✅ Да |

### Таблица обработки сетевых ошибок

| Код ошибки | Описание | Действие | Circuit Breaker |
|------------|----------|----------|-----------------|
| ENETUNREACH | Сеть недостижима (локальная проблема) | ✅ Ретрай с задержкой | ✅ Учитывается |
| ECONNRESET | Соединение сброшено | ✅ Ретрай с задержкой | ✅ Учитывается |
| ECONNREFUSED | Соединение отклонено (провайдер недоступен) | 🔄 Переключение | ✅ Учитывается, `OPEN` после threshold |
| EHOSTUNREACH | Хост недостижим | 🔄 Переключение | ✅ Учитывается, `OPEN` после threshold |
| ENOTFOUND | DNS не может разрешить хост | 🔄 Переключение | ✅ Учитывается, `OPEN` после threshold |
| ETIMEDOUT | Таймаут соединения | 🔄 Переключение | ✅ Учитывается, `OPEN` после threshold |

---

## 2. Внесенные изменения в код

### 2.1 Файл: `src/common/utils/error-extractor.util.ts`

**Добавлены методы:**

```typescript
/**
 * Check if error is a retryable network error (local network issues).
 * These errors may be temporary and worth retrying on the same model.
 */
public static isRetryableNetworkError(error: unknown): boolean {
  const code = this.extractNetworkErrorCode(error);
  if (!code) {
    return false;
  }

  // ENETUNREACH - Network unreachable (local network issue)
  // ECONNRESET - Connection reset (may be temporary)
  return code === 'ENETUNREACH' || code === 'ECONNRESET';
}

/**
 * Check if error is a provider network error (provider is unavailable).
 * These errors indicate the provider is down and we should switch to another model.
 */
public static isProviderNetworkError(error: unknown): boolean {
  const code = this.extractNetworkErrorCode(error);
  if (!code) {
    return false;
  }

  // ECONNREFUSED - Connection refused (provider is down)
  // EHOSTUNREACH - Host unreachable (provider is down)
  // ENOTFOUND - DNS lookup failed (provider doesn't exist)
  // ETIMEDOUT - Connection timeout (provider is slow/down)
  return (
    code === 'ECONNREFUSED' ||
    code === 'EHOSTUNREACH' ||
    code === 'ENOTFOUND' ||
    code === 'ETIMEDOUT'
  );
}

/**
 * Extract network error code from error object
 */
private static extractNetworkErrorCode(error: unknown): string | undefined {
  // ... implementation
}
```

### 2.2 Файл: `src/modules/router/router.service.ts`

**Изменение 1: Добавлены ретраи для сетевых ошибок**

```typescript
shouldRetry: error => {
  const errorInfo = ErrorExtractor.extractErrorInfo(error, model);
  // Retry on rate limit (429) or retryable network errors (ENETUNREACH, ECONNRESET)
  return (
    ErrorExtractor.isRateLimitError(errorInfo.code) ||
    ErrorExtractor.isRetryableNetworkError(error)
  );
},
```

**Изменение 2: Исключены 4xx из Circuit Breaker**

```typescript
} catch (error) {
  if (ErrorExtractor.isAbortError(error)) {
    throw this.handleAbortError();
  }

  const latencyMs = Date.now() - startTime;
  const errorInfo = ErrorExtractor.extractErrorInfo(error, model);

  // Only record in Circuit Breaker if NOT a client error (4xx except 429)
  // Client errors (400, 401, 403, etc.) are problems with the request/config,
  // not with the model itself, so they should not affect the circuit breaker
  if (!ErrorExtractor.isClientError(errorInfo.code)) {
    this.circuitBreaker.onFailure(model.name, errorInfo.code, latencyMs);
  }

  throw error;
}
```

### 2.3 Файл: `README.md`

**Обновлены разделы:**
- "Обработка ошибок и fallback" — добавлена логика для сетевых ошибок
- "Обработка различных кодов ошибок" — 4xx теперь помечены как "Не влияет" на Circuit Breaker
- "Сетевые ошибки" — новый раздел с таблицей
- "Примечания" — обновлены пояснения

---

## 3. Circuit Breaker состояния

- **CLOSED** — нормальная работа, запросы разрешены
- **OPEN** — circuit открыт после `failureThreshold` ошибок, запросы блокируются до истечения `cooldownPeriodSecs`
- **HALF_OPEN** — тестовые запросы для проверки восстановления
- **PERMANENTLY_UNAVAILABLE** — модель вернула 404, исключена до рестарта сервиса

---

## 4. Итоговая оценка

### Общая архитектура: ⭐⭐⭐⭐⭐ (5/5)

**Сильные стороны:**
- ✅ Четкое разделение ответственности
- ✅ Правильная обработка Circuit Breaker
- ✅ Хорошая обработка 429 с jitter
- ✅ Корректная обработка 404
- ✅ **4xx ошибки не влияют на Circuit Breaker**
- ✅ **Сетевые ошибки обрабатываются дифференцированно**

### Соответствие документации: ⭐⭐⭐⭐⭐ (5/5)

- ✅ Документация полностью соответствует коду
- ✅ Четкое описание поведения
- ✅ Понятная терминология
- ✅ Отдельный раздел для сетевых ошибок

### Надежность: ⭐⭐⭐⭐⭐ (5/5)

- ✅ Хорошо обрабатывает типичные случаи
- ✅ Сетевые ошибки обрабатываются правильно
- ✅ 4xx не влияют на Circuit Breaker
- ✅ Timeout не ретраится (правильно для длительных таймаутов)

---

## 5. Заключение

Все выявленные проблемы **исправлены**:

1. ✅ **4xx ошибки (кроме 429 и 404)** — исключены из Circuit Breaker
2. ✅ **Сетевые ошибки** — добавлена дифференцированная обработка
3. ✅ **Timeout** — задокументировано, что не ретраится (это правильно)
4. ✅ **5xx ошибки** — работают корректно (переключение без ретраев)
5. ✅ **Документация** — полностью обновлена

Система теперь более надежна и предсказуема.
